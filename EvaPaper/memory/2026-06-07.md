# 2026-06-07

- User wants the codebase read with the goal of improving paper-finding agents using an existing/public scholarly graph instead of pure keyword search.
- User clarified the graph problem should likely be split into two folds:
  1. scouting: find the full relevant paper set quickly, accurately, and cost-efficiently
  2. question answering over collected papers: for a specific question, identify which papers actually answer it
- User clarified the main research focus is static-time checking for agent interactions/specifications rather than runtime enforcement, while still needing awareness of the full AI governance state of the art.
- Repo analysis: current codebase is mostly a report archive plus a scout stub; there was no real graph-backed discovery layer.
- Implemented `scripts/paper_graph.py`:
  - OpenAlex as default public graph backend
  - optional Semantic Scholar recommendations signal
  - expands query seeds through references, citing works, and related works
  - ranks candidates for agent use
- Integrated graph discovery entrypoint into `scripts/scout.py` via `--discover`.
- Updated README with graph-backed discovery usage.
- Implemented local corpus indexing and question-to-paper retrieval:
  - `scripts/paper_corpus.py`
  - `scripts/question_map.py`
- Added explicit focus modes for retrieval/state-of-the-art views:
  - `static`
  - `runtime`
  - `behavioral`
  - `all`
- Expanded the corpus to merge both the main markdown report and `memory/agent_governance_scout_log.md`, increasing coverage to 42 indexed papers/products.
- Static-first SOTA now surfaces static-governance papers like Skilldex, GovernSpec, AST10, SkCC, Trace-Based Assurance Framework, GitHub Spec Kit/SDD, Web Skills Protocol, and SentinelAgent.
- Refactored the repo away from hardcoded OpenClaw paths:
  - added `scripts/workspace_config.py` for repo-relative shared paths
  - refactored `scripts/scout.py` to use configurable paths and explicit commit/push flags
  - refactored document generators to save outputs relative to the repo
- Added `scripts/agent_team.py` as a generic multi-agent orchestration entrypoint with roles:
  - scout
  - librarian
  - static_analyst
  - runtime_analyst
  - behavioral_analyst
  - synthesizer
- Added offline fallback in `scripts/agent_team.py`: when graph APIs are unavailable, the team run still works over the local corpus and writes `data/agent_team/last_team_run.json`.
- Added a root `Makefile` as the easiest workflow entrypoint for the repo:
  - `make help`
  - `make sota-static`
  - `make ask-static`
  - `make team-static`
  - variable-driven usage via `QUERY`, `QUESTION`, `MODE`, `TOP_K`
